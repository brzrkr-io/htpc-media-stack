---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Only monitoring namespace policies are active.
  # App namespace policies (media, downloads, tools) were removed because
  # default-deny-all + explicit allow is too fragile on a single-node home server
  # with NodePort access, host-network pods, and SNAT'd traffic.
  - monitoring-namespace.yaml
