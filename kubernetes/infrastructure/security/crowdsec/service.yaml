---
apiVersion: v1
kind: Service
metadata:
  name: crowdsec
  namespace: security
  labels:
    app: crowdsec
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: api
  selector:
    app: crowdsec
